// Root build file for the Auto Shutdown multi-loader project.
// Platform builds live in the `forge` and `neoforge` subprojects,
// which share the common sources under `src/main`.

allprojects {
    group = mod_group_id
}
